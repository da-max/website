export enum TYPE {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    DEFAULT = 'default'
}

export enum BTN_TYPE {
    TEXT = 'text'
}

export type BUTTON_TYPE = BTN_TYPE | TYPE

export enum MODIFIER {
    ROUNDED
}

export enum SIZE {
    SM,
    MD,
    LG,
    XL
}
